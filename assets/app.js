"use strict";const state={municipalities:[],regulations:[]},el=id=>document.getElementById(id),esc=v=>String(v??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c]));async function json(response){if(!response.ok)throw new Error(`Gegevens niet beschikbaar (${response.status})`);return response.json()}async function load(){const [m,r]=await Promise.all([fetch("data/municipalities-2026.json").then(json),fetch("data/regulations.json").then(json)]);state.municipalities=m.municipalities;state.regulations=r.records;el("count").textContent=m.municipalities.length;el("municipality").innerHTML='<option value="">Kies een gemeente</option>'+m.municipalities.map(x=>`<option value="${x.code}">${esc(x.name)} — ${esc(x.provinceName)}</option>`).join("")}function select(code,address){const m=state.municipalities.find(x=>x.code===code||x.code===`GM${String(code).replace(/^GM/,"").padStart(4,"0")}`||x.name.toLowerCase()===String(code).toLowerCase());if(!m)return;el("municipality").value=m.code;const rules=state.regulations.filter(x=>x.municipalityCode===m.code);el("result-title").textContent=m.name;el("badge").textContent=rules.length?`${rules.length} bronrecord${rules.length===1?"":"s"}`:"Nog geen inhoudelijke dekking";const box=el("address");box.hidden=!address;if(address)box.innerHTML=`<strong>${esc(address.weergavenaam)}</strong><br>BAG-id: ${esc(address.identificatie||"niet ontvangen")} · Gemeente: ${esc(m.name)}`;el("rules").innerHTML=rules.length?rules.map(r=>`<article class="card"><div class="meta"><span>${esc(r.regulationType)}</span><span>${esc(r.scopeType)}: ${esc(r.scopeValue)}</span><span class="pending">juridische review: ${esc(r.legalReviewStatus)}</span></div><h3>${esc(r.title)}</h3><p>${esc(r.shortDescription)}</p><p><strong>Voorwaarden:</strong> ${esc(r.conditions||"Raadpleeg de officiële bron.")}</p><p><a href="${encodeURI(r.officialInformationUrl)}" target="_blank" rel="noopener">Officiële informatie ↗</a>${r.officialRegulationUrl?` · <a href="${encodeURI(r.officialRegulationUrl)}" target="_blank" rel="noopener">Regeling ↗</a>`:""}</p><small>Broncontrole: ${esc(r.sourceVerificationStatus)} · ${esc(r.lastVerificationDate)}</small></article>`).join(""):'<p class="empty">Deze gemeente staat in de CBS-basis, maar heeft nog geen beoordeelde regelrecords. Controleer de gemeentelijke website.</p>';el("results").focus()}el("municipality").addEventListener("change",e=>select(e.target.value));el("address-form").addEventListener("submit",async e=>{e.preventDefault();const msg=el("message"),postcode=el("postcode").value.toUpperCase().replace(/\s/g,""),number=el("number").value.trim(),addition=el("addition").value.trim();msg.className="";if(!/^[1-9][0-9]{3}[A-Z]{2}$/.test(postcode)||!/^[1-9][0-9]*$/.test(number)){msg.textContent="Vul een geldige postcode en positief huisnummer in.";msg.className="error";return}msg.textContent="Officieel BAG-adres zoeken via PDOK…";try{const q=`postcode:${postcode} AND huisnummer:${number}${addition?` AND huisletter:${addition}`:""}`,data=await fetch(`https://api.pdok.nl/bzk/locatieserver/search/v3_1/free?q=${encodeURIComponent(q)}&fq=type:adres&rows=10`).then(json),docs=data.response?.docs||[],found=docs.find(d=>!addition||String(d.huisletter||d.huisnummertoevoeging||"").toLowerCase()===addition.toLowerCase())||docs[0];if(!found)throw new Error("Geen officieel BAG-adres gevonden.");const m=state.municipalities.find(x=>x.name.toLowerCase()===String(found.gemeentenaam||"").toLowerCase()||x.code===found.gemeentecode);if(!m)throw new Error("Adres gevonden, maar gemeente niet gekoppeld.");msg.textContent=`Gevonden: ${found.weergavenaam}`;select(m.code,found)}catch(error){msg.textContent=`${error.message} Kies de gemeente eventueel handmatig.`;msg.className="error"}});load().catch(error=>{el("message").textContent=error.message;el("message").className="error"});
