// Registry-backed rerun of Batch 004 to verify the former discovery bootstrap blocker is resolved.
import fs from 'node:fs/promises';
import { performance } from 'node:perf_hooks';

const read=async p=>JSON.parse(await fs.readFile(p,'utf8'));
const write=async(p,v)=>fs.writeFile(p,JSON.stringify(v,null,2)+'\n');
const batch=await read('data/research-batches/batch-004-hybrid.json');
const registry=await read('data/source-registry/bronregister-342.json');
const regByCode=new Map(registry.records.map(r=>[r.municipalityCode,r]));
const UA='WoningencheckResearchBot/1.1 (+https://woningencheck.nl)';
const keywords=['huisvest','verhuur','kamerverhuur','opkoop','splits','onttrekk','leegstand','vakantieverhuur','short-stay','shortstay','woonruimte','woningdelen','woningvorming'];
const noise=[/zaalverhuur/i,/onderwijshuisvesting/i,/grondwater/i,/weiland/i,/subsidie/i,/ouderenhuisvesting/i,/statushouder/i];
let requests=0,errors=0,active=0,maxConcurrency=0;
const fetchText=async url=>{requests++;active++;maxConcurrency=Math.max(maxConcurrency,active);try{const r=await fetch(url,{redirect:'follow',signal:AbortSignal.timeout(15000),headers:{'user-agent':UA}});const text=await r.text();return{ok:r.ok,status:r.status,url:r.url,text};}catch(e){errors++;return{ok:false,status:'error',url,text:'',error:String(e)}}finally{active--}};
const locs=xml=>[...xml.matchAll(/<loc>\s*([^<]+)\s*<\/loc>/gi)].map(m=>m[1].replaceAll('&amp;','&'));
const links=(html,base)=>{const out=[];for(const m of html.matchAll(/href=["']([^"'#]+)["']/gi)){try{out.push(new URL(m[1],base).href)}catch{}}return out};
const host=u=>{try{return new URL(u).hostname.replace(/^www\./,'')}catch{return''}};
const relevant=u=>{const t=decodeURIComponent(u).toLowerCase();return keywords.some(k=>t.includes(k))&&!noise.some(r=>r.test(t))};
async function discover(base){const cand=new Set();const add=xs=>xs.forEach(u=>{if(host(u)===host(base)&&relevant(u))cand.add(u)});const home=await fetchText(base);if(home.ok)add(links(home.text,home.url));const sm=[];const robots=await fetchText(new URL('/robots.txt',base).href);if(robots.ok)for(const m of robots.text.matchAll(/^sitemap:\s*(\S+)/gim))sm.push(m[1]);sm.push(new URL('/sitemap.xml',base).href,new URL('/sitemap_index.xml',base).href);for(const s of [...new Set(sm)].slice(0,3)){const r=await fetchText(s);if(!r.ok)continue;const ls=locs(r.text);add(ls);for(const child of ls.filter(x=>/sitemap/i.test(x)).slice(0,3)){const c=await fetchText(child);if(c.ok)add(locs(c.text));}}const list=[...cand].slice(0,12),live=[];for(const u of list.slice(0,6)){const r=await fetchText(u);live.push({url:u,status:r.status,reachable:r.ok,finalUrl:r.url})}return{candidates:list,reachable:live.filter(x=>x.reachable).map(x=>x.finalUrl||x.url)}}
const outRecords=[];let idx=0;
await Promise.all(Array.from({length:3},async()=>{while(true){const i=idx++;if(i>=batch.records.length)return;const b=batch.records[i],rr=regByCode.get(b.municipalityCode),site=(rr?.sources||[]).find(s=>s.sourceClass==='municipal-site')?.url||null;const t=performance.now();if(!site){outRecords[i]={municipalityCode:b.municipalityCode,municipalityName:b.municipalityName,registryHit:Boolean(rr),municipalSite:null,candidateCount:0,reachableCount:0,route:'manual-review-required',reason:'no-municipal-site-in-registry',durationMs:Math.round(performance.now()-t)};continue}const d=await discover(site);outRecords[i]={municipalityCode:b.municipalityCode,municipalityName:b.municipalityName,registryHit:true,municipalSite:site,candidateCount:d.candidates.length,reachableCount:d.reachable.length,candidates:d.candidates,reachableCandidates:d.reachable,route:d.reachable.length?'structured-verification':'deep-verification-needed',durationMs:Math.round(performance.now()-t)}}}));
const summary={municipalities:outRecords.length,registryHits:outRecords.filter(r=>r.registryHit).length,structuredVerification:outRecords.filter(r=>r.route==='structured-verification').length,deepVerificationNeeded:outRecords.filter(r=>r.route==='deep-verification-needed').length,manualReviewRequired:outRecords.filter(r=>r.route==='manual-review-required').length,totalCandidates:outRecords.reduce((n,r)=>n+r.candidateCount,0),networkRequests:requests,networkErrors:errors,maxConcurrency};
const output={schemaVersion:'1.0.0',batchId:'batch-004-registry-recheck',generatedAt:new Date().toISOString(),sourceBatch:'batch-004-hybrid',purpose:'Recheck the former systemic blocker using the current nationwide source registry.',records:outRecords,summary,safety:{discoveryIsNotLegalVerification:true,publicRulesCreated:0,negativeLegalConclusions:0,existingBatchUnchanged:true}};
await write('data/research-batches/batch-004-registry-recheck.json',output);
const rows=outRecords.map(r=>`| ${r.municipalityName} | ${r.registryHit?'ja':'nee'} | ${r.candidateCount} | ${r.reachableCount} | ${r.route} |`).join('\n');
await fs.writeFile('docs/research-batches/batch-004-registry-recheck.md',`# Batch 004 — hercontrole met landelijk bronregister\n\nDe oorspronkelijke Batch 004 stopte omdat onbekende gemeenten geen geverifieerde website-ingang hadden. Deze hercontrole gebruikt het inmiddels gevalideerde landelijke bronregister. Dit is discovery, geen juridische verificatie.\n\n| Gemeente | Registry-hit | Kandidaten | Live | Route |\n| --- | --- | ---: | ---: | --- |\n${rows}\n\n## Samenvatting\n\n- Gemeenten: ${summary.municipalities}\n- Registry-hits: ${summary.registryHits}\n- Structurele verificatie: ${summary.structuredVerification}\n- Diepe verificatie nodig: ${summary.deepVerificationNeeded}\n- Handmatige review: ${summary.manualReviewRequired}\n- Relevante kandidaten: ${summary.totalCandidates}\n- Netwerkverzoeken: ${summary.networkRequests}\n- Netwerkerrors: ${summary.networkErrors}\n- Max concurrency: ${summary.maxConcurrency}\n- Publieke regels: 0\n\nDe oude systemische blokkade geldt niet meer wanneer registry-hits 25/25 zijn. Gemeenten zonder relevante kandidaat blijven veilig in diepe verificatie; dit is geen conclusie dat regelgeving ontbreekt.\n`);
console.log(JSON.stringify(summary,null,2));
if(summary.municipalities!==25||summary.registryHits!==25)process.exitCode=1;
